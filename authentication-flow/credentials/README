# 🔑 Exemplo Client Credentials com Keycloak (Express + Node.js)

Este projeto demonstra o uso do **OAuth2 Client Credentials Flow** com **Keycloak**, onde:

- **Serviço A (caller)**: obtém um **access_token** do Keycloak usando `client_credentials` e chama o Serviço B.
- **Serviço B (resource)**: expõe uma API protegida que valida o token JWT usando as chaves públicas (JWKS) do Keycloak.

- Rodar o docker-compose

``
node server-a.js
node server-b.js

curl http://localhost:3002/call
``